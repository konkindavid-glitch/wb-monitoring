# Карта мониторинга

Модуль автопостинга, который системно наблюдает за полем маркетплейсов, e-commerce и AI: описывает **что** отслеживать, **как это оценивать** и **в каком порядке брать в работу**.

Не поиск по словам «Wildberries» и «Ozon». Карта задаёт пространство наблюдения по четырём осям — площадки, темы, сигналы, стоп-лист — и для каждой находки выдаёт разбор оценки и полосу очереди.

**Главный вопрос, на который обязан отвечать любой материал:**

> Селлер прочитал это — он заработает, не потеряет деньги, избежит риска или быстрее примет решение?

---

## Три решения, которые определяют конструкцию

**1. Матрица баллов определяет приоритет, а не публикацию.**
Матрица из ТЗ — взвешенная сумма, и она допускает компенсацию: слух без подтверждения набирает 115 − 50 = **65 баллов** и по шкале ТЗ попадает в «готовить пост». Поэтому матрица решает только, за что браться первым. Разрешение на публикацию остаётся за четырьмя независимыми гейтами через AND, которые живут downstream. Разбор — [docs/01-triage-scoring.md](docs/01-triage-scoring.md) §3.

**2. Пять минут там, где это окупается.**
Полный обход 89 запросов каждые 5 минут — 288 прогонов в сутки, rate limit источников и оплата пустых запросов. Класс A (5 минут) закреплён за Wildberries и детектором сбоев; остальное — по классам B–F. Разбор — [docs/02-cadence.md](docs/02-cadence.md).

**3. Расширение тематики — записанное решение, а не побочный эффект.**
Тринадцать площадок второго эшелона и темы AI, трендов и потребительского поведения добавлены сознательно 27.08.2026, с порогом входа. [docs/00-monitoring-map.md](docs/00-monitoring-map.md) §6.

---

## Как читать

| # | Документ | О чём |
|---|---|---|
| 00 | [Карта мониторинга](docs/00-monitoring-map.md) | Площадки, 11 тем и 137 подтем, 18 сигналов, 14 стоп-правил |
| 01 | [Матрица, пороги, стоп-правила](docs/01-triage-scoring.md) | 14 факторов, полосы очереди, почему приоритет ≠ публикация |
| 02 | [Частоты и heartbeat](docs/02-cadence.md) | Шесть классов частоты, десять вопросов, ускорение по событию |
| 03 | [Контракт передачи](docs/03-handoff.md) | Что уходит наружу, четыре гейта как внешний интерфейс, состояния находки |

Артефакты: [config/monitoring-map.yaml](config/monitoring-map.yaml) · [config/queries.yaml](config/queries.yaml) · [config/triage.yaml](config/triage.yaml) · [schemas/](schemas/) · [sql/001_monitoring_map.sql](sql/001_monitoring_map.sql)

---

## Что внутри

```
app.py       точка входа: цикл, расписание, тик, heartbeat, доставка
monitoring/  коллекторы, стоп-правила, факторы, матрица, разметка тем
docs/        четыре документа: карта, матрица, частоты, контракт передачи
config/      таксономия, 89 запросов, веса и пороги, реестр источников
schemas/     контракты находки и heartbeat (JSON Schema Draft 2020-12)
sql/         миграция: 6 перечислений, 9 таблиц, 19 индексов
tests/       117 тестов
tools/       проверялки согласованности
```

**Числа модуля:** 18 площадок · 11 тем · 137 подтем · 18 категорий · 18 сигналов · 89 запросов · 6 классов частоты · 14 факторов матрицы · 14 стоп-правил · 9 источников.

---

## Запуск

```bash
PYTHONUTF8=1 python app.py --onboard
```

Проверяет все источники и печатает, какие отвечают. Адреса лент меняются, и источник, отдающий ноль элементов месяцами, должен быть виден.

```bash
PYTHONUTF8=1 python app.py --once --dry-run
```

Настоящий сбор без записи в базу и без отправки: видно, что пришло, что отсеяно и с какими баллами прошло остальное.

```bash
PYTHONUTF8=1 python app.py
```

Рабочий режим: цикл по шести классам частоты. Для Cron Jobs в Амвере — `app.py --once`.

---

## Развёртывание в Амвере

Проекты созданы: `monitoring-db` (PostgreSQL, тариф Начальный + 2 ГБ диска = 326 ₽/30 дней) и `wb-monitoring` (приложение, Начальный = 290 ₽/30 дней).

**Что осталось:**

1. Завести ключ на [openrouter.ai](https://openrouter.ai) — старый ключ Anthropic из `tg-ai-news` отвечает 401.
2. Отправить код. Remote уже настроен, нужна одна команда — Git спросит логин `davidkonkin` и пароль от Амверы:

```bash
git push amvera master
```

3. Прописать переменные окружения:

| Переменная | Значение |
|---|---|
| `DATABASE_URL` | `postgresql://monitoring:ПАРОЛЬ@amvera-davidkonkin-cnpg-monitoring-db-rw:5432/monitoring` |
| `OPENROUTER_API_KEY` | новый ключ |
| `TELEGRAM_BOT_TOKEN` | из `C:\Контент Мейкер\video\.env` |
| `TELEGRAM_CHAT_ID` | оттуда же |
| `SNAPSHOT_DIR` | `/data/snapshots` — уже задана |

Миграция применяется сама при старте: она идемпотентна и переживает перезапуск.

**Про сеть.** С этой машины заблокированы `cloud.amvera.ru`, `docs.amvera.ru` и SSH на порт 22 к Амвере — рвётся рукопожатие. Консоль открывается только в Яндекс Браузере, страницам нужно 5–8 секунд. При этом `git.msk0.amvera.ru` по HTTPS отвечает нормально, поэтому `git push` работает, а `git push` по SSH — нет.

**Признаки, что заработало:** в логах `[tick] class=A`, в базе появляются строки `monitoring_runs`, в `@CMZakaz_Bot` приходит первый дайджест. Строка `[degraded]` в логах означает, что ключ модели не работает — тогда семь факторов из четырнадцати не проставляются и всё уходит в `DROP`.

---

## Проверка

```bash
PYTHONUTF8=1 python tools/validate.py
```

90 проверок согласованности «документ ↔ YAML ↔ схема ↔ SQL»: полнота запросов против ТЗ, совпадение перечислений во всех четырёх местах, арифметика матрицы на трёх эталонных случаях, валидация примеров из документов по JSON Schema. Отчёт пишется в `tools/validation-report.txt`.

```bash
PYTHONUTF8=1 python tools/check_sql.py
```

18 проверок миграции: разбор парсером PostgreSQL, состав таблиц и типов, разрешимость внешних ключей, наличие обещанных документами ограничений. Отчёт — `tools/sql-check-report.txt`.

> **Миграция нигде не применялась.** На этой машине нет ни `psql`, ни Docker, поэтому SQL проверен только статическим разбором. При развёртывании применить: `psql -f sql/001_monitoring_map.sql`.

Зависимости проверялок: `pyyaml`, `jsonschema`, `sqlglot`.

**Про кодировку.** Системная кодовая страница на этой машине — cp1251, и кириллица в консоли приходит мусором. Проверялки печатают только латиницу, а полные отчёты пишут файлами в UTF-8 — результат смотреть в файле, а не по выводу в терминал.

---

## Границы модуля

**Построено:** два коллектора — сравнение снапшотов официальных страниц и RSS отраслевых СМИ; реестр источников с онбордингом; стоп-правила; гибридный классификатор — семь факторов кодом, семь через модель; матрица и полосы очереди; heartbeat из десяти вопросов; дайджест в Telegram.

**Не входит в v1 и требует отдельного захода:**

- Telegram-каналы как источник — нужен MTProto и выделенный аккаунт;
- госреестры, картотека арбитражных дел, площадки P4;
- детектор массовости — без потока из Telegram и соцсетей ему не на чем работать;
- поисковый API — 89 запросов пока работают матчерами тем, шов под поиск готов;
- фактчекинг и четыре гейта публикации — интерфейс описан в [docs/03-handoff.md](docs/03-handoff.md) §3;
- написание постов и картинки к ним — решение записано в [спеке](docs/superpowers/specs/2026-08-27-monitoring-collector-design.md) §11.

Проработанная архитектура недостающих слоёв существует отдельно — `C:\Ведение группы в телеграм`. Словарь категорий здесь намеренно совпадает с её `event_category`, чтобы стыковка не потребовала переразметки накопленного.
